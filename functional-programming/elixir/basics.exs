IO.puts(2555) # integer 
IO.puts(0b1010) # binary
IO.puts(3.14) # float
IO.puts(true) # boolean
IO.puts(:atom) # atom
IO.puts("Hello, World!") # string
IO.puts(2 + 2) # arithmetic
IO.puts(2 == 2) # comparison
IO.puts(2 != 2 && 2==2) # boolean operators
IO.puts( false || 3)
IO.puts( 3 || true)
IO.puts( "Hello, " <> "World!") # string concatenation
IO.puts( "Hello, #{"TEST"}!") # string interpolation
